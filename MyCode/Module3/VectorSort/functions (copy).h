#ifndef FALLING_H
#define FALLING_H

#include <fstream>
#include <algorithm>
#include <string>
#include <vector>>
using namespace std; 

void swap(vector<int>& v, int x, int y);

void sortVector(vector<int>& v); 

#endif
