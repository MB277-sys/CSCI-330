#ifndef FALLING_H
#define FALLING_H

#include <fstream>
#include <algorithm>
#include <string>
#include <vector>>
using namespace std; 

void interleave(const vector<string>& v1, 
                const vector<string>& v2, 
                vector<string>& interleaved);

#endif
