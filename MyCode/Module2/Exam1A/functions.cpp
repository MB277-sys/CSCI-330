#include <iostream>
#include <cmath>
#include "functions.h"
using namespace std;

int calcScore(char rounds[], int size)
  {
  int i; 
  string Wcou("W");
  string Tou("T");
    while (i < rounds.size()) 
     {
      if (rounds.at(i) == wcou) 
        {
         numW++;
        }
      i++;
    }
   
   return numT;
}

    while (i < rounds.size()) 
     {
      if (rounds.at(i) == wcou) 
        {
         numT++;
        }
      i++;
    }
   
   return numT;
}       
   T pow(numT,2);
    
  }