#ifndef POINTER_H
#define POINTER_H

#include <fstream>
#include <algorithm>
#include <vector>
using namespace std; 

void swap(vector<int>& v, int x, int y); 
void sortVector (vector<string *>& v); 

#endif
